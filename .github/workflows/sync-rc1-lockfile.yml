name: Sync rc.1 lockfile

on:
  push:
    branches:
      - agent/sync-rc1-lockfile

permissions:
  contents: write

jobs:
  sync-lockfile:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: agent/sync-rc1-lockfile
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm
      - name: Synchronize package identity
        run: npm version 3.0.0-rc.1 --no-git-tag-version --allow-same-version
      - name: Verify package identity
        run: |
          test "$(node -p "require('./package-lock.json').version")" = "3.0.0-rc.1"
          test "$(node -p "require('./package-lock.json').packages[''].version")" = "3.0.0-rc.1"
      - name: Commit mechanical lockfile sync
        run: |
          rm .github/workflows/sync-rc1-lockfile.yml
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add package.json package-lock.json .github/workflows/sync-rc1-lockfile.yml
          git diff --cached --exit-code && exit 0
          git commit -m "Sync package lock version for rc.1"
          git push
